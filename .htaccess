# Redirect binaural-externalization to binaural-externalization/
RedirectMatch 301 ^/binaural-externalization$ /binaural-externalization/

# Optional: Force trailing slash for all directories
# RewriteEngine On
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_URI} !(.*)/$
# RewriteRule ^(.*)$ $1/ [L,R=301] 